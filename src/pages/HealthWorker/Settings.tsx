
import { Container, Title } from '@mantine/core';

const Settings = () => {
  return (
    <Container size="xl">
      <Title order={2} mb="lg">Settings</Title>
      {/* TODO: Add settings management features */}
    </Container>
  );
};

export default Settings; 