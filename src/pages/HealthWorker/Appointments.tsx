import { Container, Title } from '@mantine/core';

const Appointments = () => {
  return (
    <Container size="xl">
      <Title order={2} mb="lg">Appointments</Title>
      {/* TODO: Add appointments calendar and management features */}
    </Container>
  );
};

export default Appointments; 